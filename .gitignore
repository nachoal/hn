node_modules/
# dist/index.js is committed on purpose: `npm install -g github:nachoal/hn` installs without devDependencies,
# so there is no toolchain to build it. Run `npm run build` before committing source changes.
dist/*
!dist/index.js
.DS_Store
*.log
.env
.env.local
coverage/
*-workspace/
